{
  "id": "memory-engine",
  "name": "Memory Engine",
  "description": "MemGPT-style hierarchical memory: core memory block + archival storage with semantic search",
  "configSchema": {
    "type": "object",
    "properties": {
      "workspace": {
        "type": "string",
        "description": "Path to workspace directory"
      }
    },
    "additionalProperties": false
  }
}
